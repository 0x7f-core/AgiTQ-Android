<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Exact visible 28dp button. No transparent outer canvas. -->
    <item android:drawable="@drawable/bg_refresh_circle" />

    <!-- Same icon proportions as the original 200SPX button. -->
    <item
        android:width="18dp"
        android:height="18dp"
        android:gravity="center"
        android:drawable="@drawable/ic_refresh" />
</layer-list>
