function sma(data,p){const out=Array(data.length).fill(null);for(let i=p-1;i<data.length;i++){let s=0;for(let j=0;j<p;j++)s+=data[i-j];out[i]=s/p}return out}
function bandSignal(data,tqqq,p,mode,up,dn){const m=sma(data,p);let pos='BELOW',peak=0,armed=true,days=0,alert=false;for(let i=0;i<data.length;i++){if(m[i]==null)continue;const upv=m[i]*(1+up),dnv=m[i]*(1-dn);let next=pos;if(data[i]>=upv)next='ABOVE';else if(data[i]<dnv)next='BELOW';if(pos==='BELOW'&&next==='ABOVE'){peak=tqqq[Math.min(i,tqqq.length-1)];armed=true;days=1;alert=false}else if(pos==='ABOVE'&&next==='BELOW'){peak=0;days=0;alert=false}else if(next==='ABOVE'){days++;const tq=tqqq[Math.min(i,tqqq.length-1)];if(tq>peak)peak=tq;if(tq<=peak*.75){if(mode==='single'&&armed){alert=true;armed=false}else if(mode==='multi'){alert=true;peak=tq}else alert=false}else alert=false}pos=next}const cur=tqqq.at(-1);return{pos,days,peak,dd:peak?((cur-peak)/peak)*100:null,alert}}
